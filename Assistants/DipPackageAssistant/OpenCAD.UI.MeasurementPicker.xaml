<UserControl x:Class="OpenCAD.UI.MeasurementPicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OpenCAD.UI"
             xmlns:measures="clr-namespace:OpenCAD.OpenCADFormat.Measures;assembly=OpenCAD.OpenCADFormat"
             mc:Ignorable="d"
             Name="measurementPicker"
             d:DesignHeight="21" d:DesignWidth="90" Focusable="True">
    <Border CornerRadius="3" ClipToBounds="True" 
            BorderBrush="DimGray" BorderThickness="1" Background="White">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" Grid.ColumnSpan="3" 
                     Name="TxtAmount" 
                     Text="100"
                     Background="Transparent" BorderBrush="Transparent"/>

            <TextBlock Grid.Column="1"
                       Text="{Binding SelectedUnit.UISymbol, ElementName=measurementPicker }"
                       Foreground="DimGray"/>
        </Grid>
    </Border>
</UserControl>
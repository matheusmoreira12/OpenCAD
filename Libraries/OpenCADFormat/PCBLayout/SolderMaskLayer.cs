using OpenCAD.OpenCADFormat.MetaAnnotation;

namespace OpenCAD.OpenCADFormat.PCBLayout
{
    public sealed class SolderMaskLayer: Layer
    {
        public SolderMaskLayer(string name, Metadata metadata) : base(name, metadata)
        {
        }
    }
}
